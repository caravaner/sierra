# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sierra?schema=main"

# NextAuth
NEXTAUTH_SECRET=
AUTH_SECRET=
NEXTAUTH_URL=http://localhost:3100

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# App URLs
NEXT_PUBLIC_WEB_URL=http://localhost:3100
NEXT_PUBLIC_ADMIN_URL=http://localhost:3101

# Storage — "local" saves to admin/public/uploads/, "gcs" uploads to Google Cloud Storage
STORAGE_MODE=local
STORAGE_GCS_BUCKET=
# Base64-encoded GCS service account JSON. Leave empty to use Application Default Credentials.
# Generate with: base64 -i service-account.json | tr -d '\n'
STORAGE_GCS_CREDENTIALS=
